# Encryption Module - Versions and Providers
# This file defines the required providers and versions for the encryption module

terraform {
  required_version = ">= 1.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}
